#include <stdio.h>

/*
 * div - divide two integers
 * @a: first argument passed to the function
 * @b: second argument passed to the function
 * Return: result
 */
int div(int a, int b)
{
	return (a / b);
}
